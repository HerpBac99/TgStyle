# План реализации: Автогенерация капсул

- [x] 1. Реализация сервиса вычисления статистики использования и определения сезона




  - [x] 1.1 Создать `server/src/services/wardrobeUsageService.js`


    - Реализовать метод `extractItemIdsFromCanvas()` для извлечения ID вещей из canvasData капсулы
    - Реализовать метод `calculateUsageStats()` для вычисления usageCount каждой вещи на основе существующих капсул
    - Реализовать метод `prioritizeRarelyUsedItems()` для приоритизации вещей с usageCount 1-3 (одобрены, но используются редко)
    - Реализовать метод `getPriorityScore()` для вычисления приоритета вещи (высокий для 1-3, средний для 3+, низкий для 0)
    - Реализовать метод `getCurrentSeason()` для определения текущего сезона по месяцу
    - Реализовать метод `getCurrentMonth()` для получения названия текущего месяца на русском
    - _Requirements: 2.1, 2.2, 2.3, 10.1, 10.2, 10.3, 12.1, 12.2_

- [x] 2. Реализация сервиса проверки уникальности капсул






  - [x] 2.1 Создать `server/src/services/capsuleSimilarityService.js`

    - Реализовать метод `calculateSimilarity()` для вычисления процента схожести между капсулами
    - Реализовать метод `isUnique()` для проверки уникальности с порогом 80%
    - Реализовать метод `ensureDiversity()` для обеспечения минимум 30% различий между вариантами
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 7.4_

- [x] 3. Интеграция Gemini API в FastVLM сервер (Gemini клиент уже настроен в config.py и server.py)





  - [x] 3.1 Создать endpoint `/generate-capsules` в `fastvlm-server/server.py`


    - Реализовать обработчик POST запроса с получением вещей гардероба (все 9 полей + usageCount), текущего сезона и месяца
    - Реализовать функцию `build_capsule_generation_prompt()` для построения промпта с полными данными вещей, сезоном и инструкциями по многослойности
    - Реализовать функцию `create_capsules_with_gemini()` для вызова существующего gemini_client с конфигурацией (temperature, max_tokens, response_mime_type="application/json")
    - Реализовать функцию `parse_gemini_capsule_response()` для парсинга JSON ответа от Gemini
    - Добавить обработку ошибок и логирование
    - _Requirements: 2.1, 2.4, 2.6, 2.7, 2.8, 9.1, 9.4, 12.2_



  - [ ] 3.2 Создать промпт для Gemini с учетом всех 9 полей, сезона и умной приоритизации
    - Написать промпт с инструкцией создать 3 разных подхода: микс редко используемых + популярных, больше популярных, экспериментальная
    - Добавить информацию о текущем сезоне и месяце в промпт
    - Добавить инструкции по многослойности в зависимости от сезона (футболки = базовый слой круглый год, свитера = средний/верхний, куртки = верхний)
    - Добавить инструкцию НЕ фильтровать жестко по полю season, а учитывать контекст многослойности
    - Добавить инструкции по анализу всех 9 полей классификации (category, subtype, color, material, fit, style, season, pattern, description)
    - Добавить инструкции по обеспечению разнообразия (минимум 30% различий между вариантами)
    - Добавить инструкции по избеганию дубликатов (>80% схожести с существующими капсулами)
    - Добавить инструкции по генерации названий (максимум 3 слова на русском)
    - Добавить инструкции по созданию обоснования выбора с учетом сезона и многослойности
    - _Requirements: 2.3, 2.6, 2.7, 2.8, 5.3, 7.4, 10.2, 10.3, 10.4, 10.5, 10.6, 11.1, 11.2, 11.3, 11.4, 12.2, 12.3, 12.4, 12.5, 12.6_

- [x] 4. Реализация API endpoint для генерации капсул





  - [x] 4.1 Добавить роут POST `/api/capsules/generate` в `server/src/api/capsules.js`


    - Реализовать валидацию авторизации через Telegram WebApp
    - Реализовать загрузку всех вещей гардероба пользователя из БД с полными данными (9 полей)
    - Реализовать загрузку существующих капсул пользователя для вычисления usageCount
    - Интегрировать WardrobeUsageService для вычисления usageCount и приоритизации редко используемых вещей (1-3)
    - Интегрировать WardrobeUsageService для определения текущего сезона и месяца
    - Реализовать вызов FastVLM endpoint `/generate-capsules` с приоритизированным списком вещей, текущим сезоном и месяцем
    - Интегрировать CapsuleSimilarityService для проверки уникальности результатов
    - Реализовать обработку ошибок (недостаточно вещей, ошибка API, превышен лимит)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 5.1, 5.2, 9.1, 9.2, 9.3, 9.4, 10.1, 10.2, 10.3, 12.1, 12.2_

- [x] 5. Реализация UI компонентов на клиенте






  - [x] 5.1 Создать кнопку генерации в стиле liquid glass

    - Добавить HTML разметку кнопки в `client/index.html` в секцию capsules
    - Создать стили liquid glass в `client/css/capsules.css` (полупрозрачный фон, backdrop-filter, active state для мобильного)
    - Позиционировать кнопку внизу экрана с фиксированным положением
    - _Requirements: 1.1, 1.2, 1.3, 1.4_


  - [x] 5.2 Создать модальное окно генерации

    - Добавить HTML структуру модального окна в `client/index.html`
    - Создать стили для модального окна в `client/css/capsules.css` (overlay, карточки превью, кнопки действий)
    - Реализовать структуру для отображения 3 карточек с превью капсул, описаниями и рекомендациями
    - Добавить кнопки "Сгенерировать заново" и "Отмена"
    - _Requirements: 3.1, 3.2, 3.4, 3.5, 3.6, 8.1, 11.5_

- [x] 6. Реализация клиентских сервисов




  - [x] 6.1 Создать `client/src/modules/capsules/GenerationModal.ts`


    - Реализовать класс GenerationModal с методами show(), hide()
    - Реализовать метод onSelect() для обработки выбора капсулы
    - Реализовать метод onRegenerate() для повторной генерации
    - Реализовать рендеринг 3 карточек превью с названиями, описаниями, обоснованием (с учетом сезона и многослойности) и рекомендациями
    - Добавить обработчики событий для кликов по превью и кнопкам
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 8.1, 8.2, 8.3, 8.4, 11.5_

  - [x] 6.2 Создать `client/src/modules/capsules/CapsuleGenerationService.ts`


    - Реализовать метод `calculateUsageCount()` для вычисления usageCount каждой вещи на основе существующих капсул
    - Реализовать метод `prioritizeRarelyUsedItems()` для приоритизации редко используемых вещей (1-3)
    - Реализовать метод `getCurrentSeason()` для определения текущего сезона
    - Реализовать метод `generateCapsules()` для отправки запроса на сервер с приоритизированным списком и текущим сезоном
    - Реализовать метод `checkMinimumItems()` для проверки минимального количества вещей (3+)
    - Реализовать метод `createPreview()` для генерации превью canvas в base64
    - Добавить обработку ошибок (сеть, таймаут, недостаточно вещей)
    - Реализовать логику исключения предыдущих комбинаций при регенерации
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.9, 8.5, 9.1, 9.2, 9.3, 10.1, 10.2, 12.1_

- [x] 7. Интеграция с существующими модулями




  - [x] 7.1 Модифицировать `client/src/modules/uiCapsulesGrid.ts`


    - Добавить инициализацию GenerationModal и CapsuleGenerationService в конструкторе
    - Реализовать метод `setupGenerateButton()` для привязки обработчика клика
    - Реализовать метод `handleGenerate()` для запуска процесса генерации
    - Добавить отображение индикатора загрузки с текстом "Создаем образы для вас..."
    - Реализовать обработку результата генерации и открытие модального окна с рекомендациями
    - Реализовать обработку выбора капсулы и переход к canvas editor
    - _Requirements: 1.1, 2.4, 2.9, 3.3, 4.3, 4.4_

  - [x] 7.2 Модифицировать canvas editor для автозагрузки капсул


    - Добавить метод `loadGeneratedCapsule()` для загрузки сгенерированной капсулы
    - Реализовать метод `autoPositionItems()` для автоматического позиционирования вещей (сверху вниз: outerwear → innerwear → bodywear → legwear → footwear)
    - _Requirements: 4.4, 4.5_

- [x] 8. Создание и сохранение капсул




  - [x] 8.1 Реализовать создание капсулы через API


    - Модифицировать существующий endpoint POST `/api/capsules` для поддержки автогенерированных капсул
    - Реализовать автоматическую генерацию названия из максимум 3 слов (использовать название от Gemini)
    - Сохранять дату создания в поле createdAt
    - Сохранять источник создания "ai_generated" в поле metadata
    - Сохранять рекомендации от Gemini в поле metadata
    - Сохранять информацию о текущем сезоне в поле metadata
    - _Requirements: 4.1, 4.2, 11.6, 13.1, 13.2, 13.3, 13.4, 13.5, 13.6_

- [ ] 9. Обработка ошибок и граничных случаев
  - [ ] 9.1 Реализовать клиентскую обработку ошибок
    - Добавить уведомление "Добавьте больше вещей в гардероб для генерации капсул" при <3 вещах
    - Добавить уведомление "Не удалось сгенерировать капсулы. Попробуйте позже" при ошибке API
    - Добавить уведомление "Превышен дневной лимит генераций. Попробуйте завтра" при 429 ошибке
    - Добавить уведомление "Ошибка сети. Проверьте подключение" при сетевых ошибках
    - Добавить уведомление "Генерация заняла слишком много времени. Попробуйте снова" при таймауте
    - _Requirements: 9.1, 9.2, 9.3, 9.5_

  - [ ] 9.2 Реализовать серверную обработку ошибок
    - Добавить логирование всех ошибок генерации с контекстом (userId, wardrobeItemCount)
    - Реализовать логику повтора вызова Gemini API один раз при таймауте/500 ошибке
    - Реализовать rate limiting (максимум 10 генераций в час на пользователя)
    - Добавить обработку некорректного ответа от Gemini с повторной попыткой
    - _Requirements: 9.1, 9.3, 9.4_

- [ ] 10. Финальная интеграция и тестирование
  - Проверить полный поток: клик кнопки → генерация → отображение модального окна с рекомендациями → выбор → открытие canvas
  - Проверить работу с минимальным количеством вещей (3 вещи)
  - Проверить работу с большим гардеробом (50+ вещей)
  - Проверить умную приоритизацию: 3 разных подхода (микс редко используемых + популярных, больше популярных, экспериментальная)
  - Проверить учет текущего сезона и многослойности в генерируемых капсулах
  - Проверить что система НЕ фильтрует жестко по полю season (свитер можно использовать летом как верхний слой)
  - Проверить проверку уникальности и предотвращение дубликатов
  - Проверить корректность автоматического позиционирования вещей на canvas
  - Проверить отображение рекомендаций от Gemini с учетом сезона в модальном окне
  - Проверить регенерацию с исключением предыдущих комбинаций
  - Проверить все сценарии ошибок
  - Выполнить `npm run type-check` для проверки типов TypeScript
  - Выполнить `npm run build` для проверки сборки проекта
  - _Requirements: все_
