# TgStyle - AI Agent Guidelines

**для:** Droid и других AI coding agents  
**версия:** 2.0  
**обновлено:** 2025-10-16

---

## 🎯 Что это?

TgStyle - Telegram Mini App для анализа стиля одежды с AI.

**Три части:**
1. **Client** (TypeScript/Vite) - фронтенд
2. **Server** (Node.js/Express) - REST API
3. **FastVLM** (Python/Flask) - AI анализ

---

## ⚙️ Build & Test

```bash
npm run type-check      # Проверка типов
npm run build           # Сборка (Vite + TypeScript)
npm start              # Запуск сервера
```

**Перед коммитом ВСЕГДА:**
```bash
npm run type-check; npm run build
```

---

## 📖 AI Workflow - 7 фаз

### Фаза 1️⃣: Понимание задачи

**Получил запрос:**
```
"Добавь кнопку X на экран Y" или "Исправь баг Z"
```

**Действие:**
- Ясна задача? → переходи к Фазе 2
- Не ясна? → задай УТОЧНЯЮЩИЕ ВОПРОСЫ:
  - Где именно? В каком контейнере?
  - Какие действия? onclick, что дальше?
  - Какие связи? Нужны ли API вызовы?
  - Менять БД или типы?

---

### Фаза 2️⃣: Поиск места

**Используй теги в коде:**

```bash
# Пример: ищу где создается карточка
grep -r "#CARD-FILLED" client/src --include="*.ts" -n
# Результат: uiMenu.ts:505 setupFilledCard()
```

**Теги уже в комментариях методов:**
```typescript
/**
 * Создание карточки карусели
 * #CAROUSEL #CARD-FILLED #setupFilledCard
 */
private setupFilledCard(...) { ... }
```

---

### Фаза 3️⃣: Анализ зависимостей ⭐ КРИТИЧЕСКАЯ

**Перед кодированием ВСЕГДА отвечаешь:**

```
1. Где метод (файл + строка)
2. Можно ли существующий метод расширить
3. Кто его вызывает (grep -r "methodName")
4. Какие типы использует (HistoryItem, etc)
5. Какие ещё методы используют эти типы
6. Если меняю API/типы:
   - Нужна миграция БД
   - Нужно менять server endpoints
   - Нужно менять валидацию
```

---

### Фаза 4️⃣: План для пользователя

**Выдай конкретный план:**

```
Я планирую:

1. Изменить [файл, метод, строка]:
   - Добавить [что]
   - Причина: [почему]

2. Обновить [файл, метод]:
   - Изменить [что]
   - Зависит от: [почему связано]

СВЯЗИ:
- Метод A вызывает метод B
- Используется тип HistoryItem
- API endpoint /analysis-likes/:id

РИСКИ:
- Если поменяю HistoryItem.id, нужно обновить:
  ✓ types/api.ts interface
  ✓ validation.ts validateHistoryItem()
  ✓ history.ts loadFromStorage()

Согласен?
```

**Жди одобрения пользователя!**

---

### Фаза 5️⃣: Реализация

```
1. Открыть файлы из плана (Фаза 4)
2. Внести изменения РОВНО по плану
3. НЕ менять ничего лишнего
4. Переходить к Фазе 6
```

---

### Фаза 6️⃣: Проверка сборки

```bash
npm run type-check
npm run build
```

**Ошибки?**
- Прочитай сообщение
- Найди файл и строку
- Исправь
- Повтори build

**Нет ошибок?**
- Переходи к Фазе 7 ✅

---

### Фаза 7️⃣: РЕФЛЕКСИЯ ⭐ САМЫЙ ВАЖНЫЙ ШАГ

**Проверь ЧТО ТЫ СДЕЛАЛ:**

```
□ Сделал ровно что просили?
  
□ Все связанные методы обновлены?
  (проверил grep результаты из Фазы 3)
  
□ Можно ли проще/лучше?
  - Дублирование кода?
  - Однотипные методы?
  - Есть ли null-checks?
  - Error handling везде?
  
  Если ДА → ИСПРАВЬ СЕЙЧАС!
  
□ Согласен отправить?
```

**Примеры улучшений:**
- "loadCarouselLikeStatus и loadAndUpdateLikeStatus дублируются!"
- "Три похожих цикла, можно в один"
- "Забыл проверку null перед обращением"

---

## 🏷️ Конвенции кода

### JSDoc теги для каждого метода

```typescript
/**
 * Описание метода
 * #TAG1 #TAG2 #TAG3
 */
private methodName(): void {
  try {
    // Код
  } catch (error) {
    logger.error('Error message', error);
  }
}
```

**Теги = быстрый поиск grep:**
```bash
grep -r "#LIKES" --include="*.ts" --include="*.js" -n
```

### Правила

✅ **DO:**
- Singleton паттерн (экспортируем один экземпляр)
- Строгая типизация TypeScript
- ВСЕГДА try-catch для async
- Теги в JSDoc комментариях
- Читай файл перед редактированием

❌ **DON'T:**
- `import *` - только явные импорты
- console.log - только logger
- Менять файл без grep проверки связей
- Забывать null-checks
- Игнорировать TypeScript ошибки

---

## ✅ Checklist завершения

- [ ] npm run type-check ✓
- [ ] npm run build ✓
- [ ] Grep проверка связей ✓
- [ ] Нет console.log ✓
- [ ] Теги в JSDoc ✓
- [ ] Рефлексия пройдена ✓

---

## 📞 Когда просить помощь

❓ Не понимаю что делать  
❓ Grep показал 5+ файлов - потеряюсь в зависимостях  
❓ Не уверен нужна ли миграция БД  
❓ Конфликт: два подхода, какой выбрать?

---

## 🎯 Помни

> **Фаза 7 (Рефлексия) - ОБЯЗАТЕЛЬНА!**
>
> Если просто написал и сказал "готово" - код будет хуже.
>
> Всегда проверяй:
> - Можно ли проще?
> - Не забыл ли я?
> - Есть ли баги?
> - Могу оптимизировать?

---

**Last Updated:** 2025-10-16  
**For:** Droid & AI agents
